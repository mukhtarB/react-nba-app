import React from "react";

import style from './button.module.css';

const Button = (props) => {
    return (
        <div>
            Button Component
        </div>
    )
}

export default Button;