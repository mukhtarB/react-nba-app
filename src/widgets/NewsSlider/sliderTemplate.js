import React from "react";

const SliderTemplate = (props) => {
    // console.log(props)

    return (
        <div>
            From Slider Template
        </div>
    )
}

export default SliderTemplate;